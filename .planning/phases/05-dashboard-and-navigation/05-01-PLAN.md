---
phase: 05-dashboard-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/components.js
  - css/theme.css
  - css/layout.css
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "A sticky nav bar with BRx branding and grouped dropdown menus appears on every page"
    - "Clicking any nav link navigates to the correct page regardless of current page depth (root, /docs/, /models/, /research/)"
    - "Pressing Cmd/Ctrl+K on any page opens an in-page quick-jump overlay that filters all pages by typed text"
    - "The active page is visually indicated in the nav bar based on the data-page attribute"
    - "Typography, spacing, and transitions are tightened across all pages for Bloomberg terminal density"
  artifacts:
    - path: "js/components.js"
      provides: "Global nav bar injection + quick-jump overlay for all pages"
      min_lines: 150
    - path: "css/theme.css"
      provides: "Updated design tokens with --nav-height, nav bar CSS vars"
      contains: "--nav-height"
    - path: "css/layout.css"
      provides: "Nav bar layout styles, dropdown styles, quick-jump overlay styles"
      contains: ".global-nav"
  key_links:
    - from: "js/components.js"
      to: "every HTML page"
      via: "data-page and data-base body attributes"
      pattern: "getAttribute.*data-page"
    - from: "js/components.js"
      to: "css/layout.css"
      via: "CSS classes for nav bar and quick-jump"
      pattern: "global-nav|quick-jump"
    - from: "css/layout.css"
      to: "css/theme.css"
      via: "CSS custom properties"
      pattern: "var\\(--nav-height\\)"
---

<objective>
Build the global navigation system that replaces the breadcrumb bar on all 38 pages with a persistent sticky nav bar (BRx branding, grouped dropdowns, active page indicator) and a quick-jump overlay (Cmd/Ctrl+K). Apply Bloomberg terminal polish to the design tokens.

Purpose: Enable any-to-any page navigation without returning to a hub, and provide power-user quick-jump for fast findability -- the core navigability improvement for the research tool.
Output: Rewritten components.js (nav bar + quick-jump), updated theme.css (design tokens), updated layout.css (nav bar + quick-jump styles)
</objective>

<execution_context>
@/Users/diego/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-and-navigation/05-CONTEXT.md
@.planning/phases/05-dashboard-and-navigation/05-RESEARCH.md

@css/theme.css
@css/layout.css
@css/docs.css
@css/models.css
@js/components.js

<interfaces>
<!-- Key patterns the executor needs from the existing codebase -->

From every HTML page (38 pages):
```html
<body data-page="dcf" data-base="..">
  <nav class="breadcrumb-bar" id="breadcrumb-bar"></nav>
</body>
<script src="js/components.js"></script>  <!-- or ../js/components.js for subdirs -->
```

From js/components.js (HIERARCHY map -- reuse these page identifiers):
```javascript
var HIERARCHY = {
  'dashboard': ['Dashboard'],
  'structure': ['Structure Diagram'],
  'compare': ['Covenant Comparison'],
  'glossary': ['Glossary'],
  'search': ['Search All Docs'],
  'scenarios': ['Scenarios'],
  'doc1': ['Documents', 'DOC 1 - Covenant Strip'],
  // ... 40+ entries mapping data-page -> [group, label]
  'dcf': ['Models', 'DCF Valuation'],
  'pf-recovery': ['Models', 'Pro-Forma Recovery'],
  // etc.
};
```

From css/theme.css (:root variables):
```css
--breadcrumb-height: 40px;  /* REPLACE with --nav-height */
--bg: #0a0e17;
--surface: #111827;
--surface-2: #1a2236;
--border: #2a3654;
--blue: #3b82f6;
--text: #e2e8f0;
--text-muted: #8899b4;
--text-dim: #5a6d8a;
```

CSS files referencing --breadcrumb-height (must update):
- css/layout.css line 26: height: var(--breadcrumb-height)
- css/docs.css lines 17,18,111,337,338: sticky sidebar + scroll-margin calculations
- css/models.css line 17: uses --header-height (undefined, should use --nav-height)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite components.js -- global nav bar with dropdowns + quick-jump overlay</name>
  <files>js/components.js</files>
  <action>
Completely rewrite components.js as an IIFE (ES5, `var` throughout, 'use strict'). Keep the same file -- it is already loaded via script tag on all 38 pages.

**Nav Bar Structure:**
Build a sticky top nav bar that replaces the breadcrumb bar. Inject into `#breadcrumb-bar` element (keep the ID for backward compatibility but add class `global-nav`).

Nav bar contents (left to right):
1. **BRx logo/brand** -- text "BRx" styled as bold brand mark, links to `base + '/index.html'`
2. **Site title** -- "AMC Debt Navigator" as smaller text next to logo
3. **Dropdown groups** (5 groups):
   - **Documents** (11 pages): docs-hub, doc1 through doc7 (including doc6-ex, doc7-ex)
   - **Models** (11 pages): models-hub, dcf, pf-recovery, waterfall, debt-service, financials, revenue-build, comps, leverage, exchange, pik
   - **Research** (10 pages): research hub + 8 research articles + court-solutions
   - **Scenarios** (2 pages): scenarios, scenario-detail
   - **Tools** (4 pages): search, compare, structure, glossary

Each dropdown group: a button that toggles a dropdown panel on click. Dropdown panel shows page links as a list. Use `data-base` attribute for all href resolution.

4. **Active page indicator**: Match current `data-page` against the NAV_GROUPS entries. Highlight the parent group button (blue bottom border) and bold the active page within its dropdown. Use `data-page` attribute from `document.body`.

5. **Quick-jump trigger button** (right side): A small search icon + "Cmd+K" hint text.

**Dropdown Behavior:**
- Click group button to toggle dropdown open/closed
- Click outside any dropdown to close all
- Only one dropdown open at a time
- Dropdowns positioned below the nav bar, aligned to their parent button

**Quick-Jump Overlay (full feature):**
- Triggered by Cmd+K (Mac) / Ctrl+K (all platforms), OR clicking the nav bar search icon
- Full-screen fixed overlay with semi-transparent backdrop (z-index: 1000)
- Text input at top, auto-focused
- Flat list of ALL navigable pages (use the same HIERARCHY map data)
- Each item: label + group tag + href
- Filter on keystroke using case-insensitive substring match on label + keywords
- Enter navigates to first visible result; click navigates to clicked item
- Escape closes overlay
- Do NOT navigate away to search.html on Ctrl+K anymore (replace old behavior)

**HIERARCHY data to include (all 38 pages with labels, groups, href patterns, keywords):**
Build from the existing HIERARCHY map but restructure for nav. Include keywords for quick-jump filtering (e.g., "dcf" has keywords "dcf wacc valuation discount cash flow").

**Important constraints:**
- ES5 only: use `var`, no arrow functions, no template literals, no `let`/`const`
- Use string concatenation for HTML building (not template literals)
- All hrefs use `base + '/path'` pattern where base = `document.body.getAttribute('data-base') || '.'`
- Do NOT fetch any JSON data -- nav bar is pure static HTML/links
- Close dropdowns when a link is clicked (navigate away)
  </action>
  <verify>
    <automated>node -e "var f=require('fs').readFileSync('js/components.js','utf8'); var checks=['global-nav','quick-jump','NAV_GROUPS','data-page','data-base','BRx','Cmd','Ctrl','keydown']; var missing=checks.filter(function(c){return f.indexOf(c)===-1}); if(missing.length) throw 'Missing: '+missing.join(', '); console.log('All nav patterns present')"</automated>
  </verify>
  <done>
    - components.js builds a global nav bar with BRx branding, 5 dropdown groups, and active page indicator
    - Quick-jump overlay opens on Cmd/Ctrl+K with page filtering
    - All 38 pages receive the nav bar via the existing #breadcrumb-bar container
    - Nav links resolve correctly at all directory depths via data-base attribute
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CSS -- nav bar styles, quick-jump overlay, Bloomberg polish</name>
  <files>css/theme.css, css/layout.css, css/docs.css, css/models.css</files>
  <action>
**theme.css changes:**
1. Replace `--breadcrumb-height: 40px` with `--nav-height: 48px` in `:root`
2. Keep `--breadcrumb-height` as an alias: `--breadcrumb-height: var(--nav-height)` for any pages we miss
3. Add nav-specific color tokens:
   - `--nav-bg: #0d1117` (slightly darker than --surface for visual separation)
   - `--nav-border: #1f2937`
   - `--nav-hover: rgba(59,130,246,0.08)`
   - `--nav-active: rgba(59,130,246,0.15)`
4. Bloomberg polish refinements:
   - Tighten card padding: `.card` padding from `var(--space-lg)` (24px) to `var(--space-md)` (16px)
   - Add `.card` transition: `transition: border-color 0.15s, box-shadow 0.15s`
   - Add subtle box-shadow on card hover: `box-shadow: 0 2px 8px rgba(0,0,0,0.3)`
   - Tighten `.metric` padding from `12px 20px` to `10px 14px`

**layout.css changes:**
1. Replace entire `.breadcrumb-bar` block with `.global-nav` styles:
   - `position: sticky; top: 0; z-index: 50`
   - `background: var(--nav-bg); border-bottom: 1px solid var(--nav-border)`
   - `height: var(--nav-height); padding: 0 24px`
   - `display: flex; align-items: center; gap: 0`
   - `font-size: 11px`
2. Add nav brand styles: `.nav-brand` (BRx bold + site title)
3. Add nav group button styles: `.nav-group-btn` (uppercase, 11px, letter-spacing, hover/active states)
4. Add dropdown panel styles: `.nav-dropdown` (position absolute, top 100%, background --surface, border, border-radius, box-shadow, min-width 220px, z-index 100, max-height 70vh, overflow-y auto)
5. Add dropdown item styles: `.nav-dropdown a` (block, padding 8px 16px, hover background, active bold + blue color)
6. Add quick-jump overlay styles:
   - `.quick-jump-backdrop`: fixed, inset 0, background rgba(0,0,0,0.6), z-index 1000
   - `.quick-jump-modal`: fixed, top 20%, left 50%, transform translateX(-50%), width 520px, max-height 60vh
   - `.quick-jump-input`: full-width input, 16px font, dark background
   - `.quick-jump-results`: list of results with hover states
   - `.quick-jump-item`: padding 10px 16px, border-bottom
   - `.quick-jump-item.active` (keyboard-selected): blue left border + subtle bg
   - `.quick-jump-group`: small tag/badge showing the nav group
7. Keep ALL existing breadcrumb-bar responsive rules but update class references
8. Add smooth transitions to dropdowns: `opacity 0.15s, transform 0.15s`

**docs.css changes:**
Replace all instances of `var(--breadcrumb-height)` with `var(--nav-height)`:
- Line 17: `top: calc(var(--nav-height) + 16px)`
- Line 18: `max-height: calc(100vh - var(--nav-height) - 32px)`
- Line 111: `scroll-margin-top: calc(var(--nav-height) + 16px)`
- Line 337: `top: calc(var(--nav-height) + 16px)`
- Line 338: `max-height: calc(100vh - var(--nav-height) - 32px)`

**models.css changes:**
- Line 17: Replace `var(--header-height)` with `var(--nav-height)`:
  `top: calc(var(--nav-height) + 16px)`

**Do NOT modify:** hubs.css (will be superseded by dashboard.css in Plan 02), research.css, crosslink.css
  </action>
  <verify>
    <automated>node -e "var t=require('fs').readFileSync('css/theme.css','utf8'); var l=require('fs').readFileSync('css/layout.css','utf8'); var d=require('fs').readFileSync('css/docs.css','utf8'); var m=require('fs').readFileSync('css/models.css','utf8'); var errs=[]; if(t.indexOf('--nav-height')< 0) errs.push('theme: missing --nav-height'); if(l.indexOf('.global-nav')< 0) errs.push('layout: missing .global-nav'); if(l.indexOf('.quick-jump')< 0) errs.push('layout: missing .quick-jump'); if(d.indexOf('--breadcrumb-height')>=0) errs.push('docs: still uses --breadcrumb-height'); if(m.indexOf('--header-height')>=0) errs.push('models: still uses --header-height'); if(errs.length) throw errs.join('; '); console.log('CSS updates verified')"</automated>
  </verify>
  <done>
    - --nav-height replaces --breadcrumb-height in all CSS files
    - .global-nav styles support sticky nav bar with dropdown menus
    - .quick-jump styles support the full-screen overlay modal
    - Bloomberg polish applied: tighter spacing, subtle transitions, data-dense feel
    - docs.css and models.css sticky sidebars use correct --nav-height variable
  </done>
</task>

</tasks>

<verification>
Open any existing page (e.g., models/dcf.html, docs/doc1-covenant-strip.html, search.html) in a browser:
1. Sticky nav bar appears at top with "BRx" branding and 5 dropdown groups
2. Click each dropdown group -- panel appears with correct page links
3. Active page is visually indicated (blue highlight on group, bold on page)
4. Click a nav link -- navigates to correct page (test from root and subdirectory pages)
5. Press Cmd/Ctrl+K -- quick-jump overlay appears with search input
6. Type "dcf" -- results filter to DCF-related pages
7. Press Enter -- navigates to first result
8. Press Escape -- overlay closes
9. Sticky sidebars on doc pages and model input panels still position correctly (not overlapping nav)
</verification>

<success_criteria>
- Every page in the site shows a persistent global nav bar with BRx branding
- All 38 pages are reachable via the nav bar dropdowns
- Quick-jump overlay works on every page via Cmd/Ctrl+K
- Nav links resolve correctly from pages at all directory depths (root, /docs/, /models/, /research/, /scenarios/)
- No CSS breakage on sticky sidebars in doc reader or model input panels
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-and-navigation/05-01-SUMMARY.md`
</output>
